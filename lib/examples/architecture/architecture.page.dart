import 'package:flutter/widgets.dart';

class ArchitecturePage extends StatelessWidget {
  const ArchitecturePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Architecture Page"),
      ),
    );
  }
}
