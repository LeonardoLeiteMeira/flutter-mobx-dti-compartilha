import 'package:flutter/material.dart';

class ConnectivyPage extends StatelessWidget {
  const ConnectivyPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Connectivy Page"),
      ),
    );
  }
}
